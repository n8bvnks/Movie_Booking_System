spring:
  application:
    name: booking-service
  datasource:
    url: jdbc:mysql://comp713-s2-2026.c5m2mi6uava2.ap-southeast-2.rds.amazonaws.com:3306/comp713_17988896
    username: c713_17988896
    password: ${DB_PASSWORD:}
    hikari:
      maximum-pool-size: 2
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: ${SERVER_PORT:8082}

movie:
  service:
    url: ${MOVIE_SERVICE_URL:http://localhost:8083}

management:
  endpoint:
    health:
      show-details: never